import React from 'react';

const StartStop = () => (
    <div className='start-stop'>
        <button>Start</button>
        <button>Stop</button>
        <button>Reset</button>
    </div>
);

export default StartStop;